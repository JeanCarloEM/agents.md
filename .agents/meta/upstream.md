# Evolução upstream

Subordinado a `cli.md`, cenário `../scenarios/governance/upstream-sharing/scenario.md`, `MN-OUT` e `MN-VAL`. Abrange descoberta de upstream de AGENTS.md, atualização, deduplicação, sanitização, proposta, publicação autorizada, registro e reavaliação. Configuração declara somente origem, canal, timeout, limites, cache e hooks; segredo permanece externo. Cliente público classifica rede, HTTP, sintaxe, servidor e rate limit; retry é limitado a leitura idempotente. Sem autorização, comando produz artefato revisável e não muda serviço externo.
